{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }],
    "hooks": {
        "build": "prisma generate"
    }
}